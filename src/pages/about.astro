---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About" description="Who is burnedthoughts? Not telling.">
  <article class="about">
    <h1 class="heading">About</h1>
    <div class="body">
      <p>
        I'm <strong>burnedthoughts</strong> — a person on the internet who writes
        sometimes. That's about as much as you'll get.
      </p>
      <p>
        I build software for a living. I read too much. I think about systems,
        incentives, and why things are the way they are. Occasionally I write
        those thoughts down before they disappear.
      </p>
      <p>
        This site exists because I wanted a corner of the web that's mine — no
        platform, no profile, no data broker sitting between me and the page.
        Just HTML, served fast.
      </p>
      <p>
        If you want to follow along, grab the <a href="/rss.xml">RSS feed</a>.
      </p>
    </div>
  </article>
</BaseLayout>

<style>
  .about {
    max-width: var(--max-width);
  }

  .heading {
    font-family: var(--font-serif);
    font-size: var(--font-size-2xl);
    font-weight: 600;
    line-height: var(--line-height-heading);
    letter-spacing: var(--letter-spacing-tight);
    margin-bottom: var(--space-xl);
  }

  .body {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    line-height: var(--line-height-body);
  }

  .body a {
    color: var(--color-accent);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 2px;
  }

  .body a:hover {
    color: var(--color-accent-hover);
  }
</style>
